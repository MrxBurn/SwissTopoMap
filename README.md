# Swiss Topo - React Map

This project serves as the frontend for the Swiss Topo aplication.

### Tech stack:

- React
- ESLint

### How to run

- `docker-compose up` to run the backend locally

- `yarn dev` to run the React App locally
